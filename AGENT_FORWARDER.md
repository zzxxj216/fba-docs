# 货代沟通智能体 设计（建仓后 → 询价 → 比价 → 生成发送）

> 现有 DESIGN.md 覆盖"建仓后文件准备（生成托书/报关/投保…）"。本文件设计**新增的货代沟通自动化层**：
> 建仓后按批次智能询价货代、按聊天往返收集回复、提取对比、人工拍板、生成并发送文件。
> **半自动**：智能体干起草/提取/对比，关键决策（发询价、选货代、发文件）卡人工确认。

## 一、渠道：qiweapi（第三方企微云端实例）

- 平台：`http://manager.qiweapi.com/qiwe/api/qw/doApi`，统一 POST，Header `X-QIWEI-TOKEN`，body `method` 区分操作（如 `msg/sendText`）。
- 收消息：Webhook 回调 → 本系统 `POST /api/qiwe/callback`，解析货代消息，按发送方匹配到货代+进行中的询价。
- 封装：新增 `app/qiwe_client.py`（仿 `sellfox_client`：统一 POST + token + 限流/重试）。凭据进 `.env`（`QIWE_TOKEN` 等，勿提交）。
- **风险/前提**：①云端实例需保持登录、异地登录触发安全保护（封号风险）；②货代须是该企微实例的**外部联系人或群成员**；③Webhook 需**公网可达**（本地开发要内网穿透/部署到公网）。

## 二、数据模型（新增）

- **Forwarder 扩展**：`qiwe_external_userid`（货代外部联系人id）/ `qiwe_chat_id`（群id）/ `is_default`（默认货代）/ 绑定维度（品牌/目的国/路线）。
- **Inquiry 询价单**：`batch_id`、`status`、`channel_pref`、`content_snapshot`（发出的询价正文+结构化要素）、`target_forwarder_ids`（混合：默认1家 or 多家比价）、时间。
- **InquiryQuote 报价**：`inquiry_id`、`forwarder_id`、`raw_message`（原文）、提取字段（`price`/`currency`/`eta`/`cutoff`截关/`channel`渠道/`valid_until`）、`attachments`、`extract_confidence`、`is_chosen`。
- **ForwarderMessage 消息流水**：`forwarder_id`、`direction`(in/out)、`content`/`media`、`ts`、关联 `inquiry_id`/`batch_id`（沟通留痕+给智能体上下文）。
- **批次沟通状态**：落在 Batch 或新表（见状态机）。

## 三、批次货代沟通状态机

```
待询价 →〔智能体起草·人工确认〕→ 询价已发 → 收集报价中
  →〔智能体提取+多家比价·人工拍板选货代〕→ 已选货代
  →〔生成托书等文件(复用现有引擎)·人工确认〕→ 文件已发 → 完成
```
- 收集报价中：webhook 每收到一条货代回复，智能体提取并更新对应 InquiryQuote；齐了/超时 → 进比价。
- 任意环节可回退（货代改报价、补资料）。

## 四、智能体职责（claude-opus-4-8）与人工确认点

| 智能体自动做 | 人工确认 |
|---|---|
| 起草询价消息（按批次数据+模板，措辞合适） | **发询价前**（看草稿、改、点发） |
| 从货代企微消息提取结构化报价（价格/时效/截关/渠道/附件） | — |
| 多家比价汇总 + 推荐选优（带理由） | **选哪家货代**（拍板） |
| 核对货代回传数据 vs 系统批次（箱数/体积/重量/目的仓），标差异 | 差异处置 |
| 提取货代回传文件（提单/装箱确认/账单）关键字段、归档 | — |
| 生成托书等（复用 excel_engine） | **发文件前**（看、点发） |

## 五、对比提取（用户选定三项）

1. **多家货代比价选优**：同批询多家 → 汇总各家 price/eta/cutoff/渠道 → 排序+推荐 → 人工选。
2. **核对货代数据 vs 系统**：货代发来的箱单/订舱信息逐项 diff 系统批次数据，标不一致。
3. **提取货代发来的文件**：附件（提单/装箱确认/账单）自动归档 + 抽关键字段。

## 六、分阶段路线

- **阶段0 打通管道**：`qiwe_client` + `/api/qiwe/callback` webhook + 货代↔外部联系人/群映射；目标=能从系统发一条消息给货代、能收到货代回复并落库。**先验证这个**。
- **阶段1 询价闭环（MVP核心）**：建仓批次 → 起草询价 →〔确认〕→ 发 → 收集回复 → 提取报价 → 比价展示 →〔选货代〕。
- **阶段2 核对 + 文件提取**：货代数据 diff 系统；附件归档抽字段。
- **阶段3 生成 + 发送**：选定货代后生成托书等（复用引擎）→〔确认〕→ 企微发货代。
- 贯穿：沟通状态机 + 消息留痕 + 前端"批次沟通"面板。

## 七、待确认

- qiweapi 的 `X-QIWEI-TOKEN`、已登录企微实例是否就绪？Webhook 公网地址怎么解决（部署/穿透）？
- 询价模板字段（已选：目的仓FC、价格、重量等；还要箱数/体积/品名/时效/起运地？）。
- 货代默认绑定维度（按品牌？目的国？路线？）+ 比价时默认发哪几家。
- "收集报价"齐没齐的判定（等齐N家 / 超时X小时进比价）。
