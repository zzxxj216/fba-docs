# FBA 发货文件管理系统 —— 设计文档

> 版本 v1.0（2026-06-12）· 状态：待评审
> 目标：替代两套影刀 RPA（跨运通/盈和），把建仓后文件准备（托书、报关、投保、采购合同、9810）统一为"拉取批次 → 核对 → 一键生成 → 归档"。

---

## 1. 设计原则

1. **数据只进一次**：货件数据从赛狐 API 自动拉取；产品报关主数据、工厂、公司主体在系统里各维护一份，所有文件从同一份数据生成。不再出现"报关反读托书"的链式依赖。
2. **模板与代码分离**：货代原版 Excel 模板原样上传，每个模板只配一份"映射"。货代换模板 = 重新上传 + 改映射，零代码改动。
3. **规则皆配置**：日期、编号、净重扣减、含税加价、保价档位——现在散落在 RPA 代码里的所有规则，全部变成界面可改的配置项。
4. **生成前必校验**：批次总量 = Σ货件申报量、产品主数据缺字段、单价为空、模板缺映射——问题在生成前暴露，而不是在海关。

## 2. 总体架构

```
┌─ 浏览器（Vue3 单页，局域网可共享）─────────────────────────┐
│  批次管理 │ 产品库 │ 工厂管理 │ 公司主体 │ 货代&模板 │ 规则配置 │
└──────────────────────┬────────────────────────────────┘
                       │ HTTP (JSON)
┌──────────────────────▼────────────────────────────────┐
│  FastAPI 后端                                           │
│  ├─ sellfox_client   赛狐认证+签名（独立小模块，~200行）    │
│  ├─ sync_service     批次/货件/箱级数据同步                │
│  ├─ rule_engine      日期/编号/净重/加价/保价 规则计算       │
│  ├─ field_registry   字段字典（模板映射的唯一取值入口）       │
│  ├─ excel_engine     模板填充（坐标定位 + 锚点行定位）        │
│  └─ SQLite 单文件库（备份=拷贝 data.db）                   │
└────────────────────────────────────────────────────────┘
        输出: output/{批次}/{货件}/xxx.xlsx （按批次归档，支持打包下载）
```

**与 multi-channel-api 的关系**：不依赖那个项目运行。赛狐的认证逻辑很小（token + HMAC 签名 + 1次/秒限流），本系统内置一个独立的 `sellfox_client`，凭据从本系统的 `.env` 读取（与 multi-channel-api 共用同一组 client_id/secret）。

## 3. 数据模型

### 3.1 业务数据（从赛狐同步 + 少量人工补录）

```
Batch 批次（= 赛狐 STA 入库计划）
├─ inbound_plan_id, 店铺主体id, 品牌, 站点(US/EU…), 状态
├─ base_date 基准日期(发货周五), contract_date 合同日期(规则算出, 可改)
├─ factory_id 本批供货工厂（默认取店铺主体配置，可改）
└─ Shipment 货件（= 赛狐 shipment，每个目的仓一个）
   ├─ amazon_shipment_id(FBA号), ship_sn(发货单号), reference_id
   ├─ fc_code, fc_address(API直接给，含街道/城市/州/邮编)
   ├─ carton_num 箱数, weight, volume, 预计到仓窗口
   ├─ forwarder_id（按赛狐 logisticProviderName 自动匹配）
   ├─ forwarder_order_no 货代单号（人工回填，投保用）
   ├─ status: 已同步→已核对→已生成→已投保→完成
   ├─ ShipmentItem 明细行（SKU 维度）
   │  └─ product_id(按MSKU匹配产品库), msku/fnsku/asin, qty,
   │     box_count, qty_per_box, 箱规cm, customs_unit_price, purchase_cost
   └─ ShipmentBox 逐箱（外箱标/托书箱明细用）
      └─ box_id(亚马逊箱号), sku, qty, 箱规in, 箱重lb
```

### 3.2 主数据（系统内维护）

**Product 产品报关主数据**（首次从"商品列表-报关.xlsx"导入，之后界面维护）
- sku（=MSKU 关联键）、**5 种品名变体**：中文报关名 / 英文报关名 / 合同用品名 / 发票箱单用名 / 用途功能名
- hs_code、申报要素、材质、用途、品牌、型号
- 单箱重量kg（报关毛重用）、申报单价默认值、采购成本默认值
- （数量、箱规以赛狐货件数据为准，产品库只存默认/兜底值）

**Brand 品牌（绑定关系的主键，据"店铺主体-工厂信息库.xlsx"确认）**
- 品牌名、缩写（默认取前2位大写，可改）
- **绑定：默认工厂 + 店铺主体**（一个工厂可服务多个品牌；品牌→主体一对一）
- 赛狐店铺名（如 Serenorch-US）→ 品牌+站点 的匹配规则
- 初始化：直接导入"店铺主体-工厂信息库.xlsx"（品牌-工厂 sheet + 星盟 sheet）

**Factory 工厂（独立模块）**
- 名称、缩写（LG/YY/ZE…）、地址、电话、联系人、税号、银行信息、备注
- 界面：工厂管理页独立 CRUD，支持 Excel 一次性导入存量
- 删除保护：被历史批次引用过的只能停用

**Company 公司主体**（店铺公司 + 外贸主体[星盟,缩写SA]都在这张表，type 区分）
- 中/英文名、中/英文地址、统一社会信用代码、电话、银行信息
- **编号规则模板**：如 `BY-SA-{brand2}-{fc}-{date}`、`{brand2}-{fc}-{date}`（占位符见 §5）
  - 已确认线索：二级合同（卖星盟）编号带 `SA-` 前缀（`SA-SE-US-日期`），一级不带（`SE-US-日期`），待最终确认
- **保价档位**：投保申报倍率（×1.5×1.1 / ×1.13 / ×1）
- **出口卖方**：报关合同卖方是自己还是外贸主体（星盟）
- 默认起运港、默认货源地（萧山/杭州…）

**Forwarder 货代 & Template 模板**
- 货代：名称（与赛狐 `logisticProviderName` 匹配，如"跨运通"）、联系方式
- 模板：归属（货代/内部）、文件类型（托书/报关/投保/采购合同/9810/其他）、
  模板文件、**生成粒度**（每货件一份 / 每批次一份 / 每货件一行）、映射配置、文件命名规则

### 3.3 规则配置（RuleConfig，全局 + 可按主体覆盖）

| 配置项 | 默认值（来自现 RPA） |
|---|---|
| 发货基准日 | 下一个周五 |
| 合同日期 | 基准日的上月同日（31→30） |
| 净重扣减 | 每箱 0.5 kg |
| 增值税系数 | 1.13 |
| 二级合同加价 | 1.10 |
| 保价档位 | 按公司主体（三档） |
| 报关常量 | 原产国=中国、运抵国=美国、单位=盒、币制=USD、包装=纸箱、监管方式=跨境电商出口海外仓、征免=照章征税 |
| 投保常量 | 海运、卡车派、FBA、保上架、起运=宁波、无易碎品、保险币种=人民币、比例=100% |

## 4. 赛狐同步方案

**触发**：批次管理页点「同步」→ 列出近期 STA 入库计划（`/api/inbound/plan/page.json`）→ 选择一个导入。

**同步链**（全部只读，1次/秒限流，自动重试）：

```
getInboundPlan.json        → 批次 + 货件清单(amazonShipmentId)
inbound/shipment/detail.json → 每货件: referenceId、FC+完整地址、箱数、
                              逐箱明细(inboundCartonVos)、关联发货单号(shipSnList)
fba/shippingOrder/detailByShipSn.json → 明细行: SKU/数量/每箱数/箱规cm/
                              申报单价/采购成本 + 货代名称(自动匹配货代)
```

**匹配与校验（同步完成即跑）**：
- MSKU → 产品库匹配；未匹配的列出来，可现场补建产品
- Σ货件申报量 ↔ 发货单数量一致性
- 产品库必填字段齐全（HS编码、报关名、材质…）
- 申报单价/采购成本为空提醒
- 同一批次重复同步 = 增量更新（已回填的货代单号等人工字段不被覆盖）

**离线兜底**：保留 Excel 导入入口（发货单导出 + Send to Amazon 文件），API 不可用时手动导入，数据落同一模型。

## 5. 字段字典与编号占位符

模板映射只能引用字段字典里的路径（系统内可视化查看、点击复制）：

| 命名空间 | 示例 |
|---|---|
| `batch.*` | 品牌、站点、合同日期、基准日期 |
| `shipment.*` | fba_id、reference_id、fc_code、fc_address_line1/city/state/zip、箱数、总毛重、总净重、总体积、货代单号 |
| `item.*` | 5种品名、hs_code、qty、box_count、qty_per_box、箱规、申报单价、金额、材质、用途、品牌、图片 |
| `box.*` | 箱号、内装sku、数量、箱规in/cm、箱重lb/kg |
| `company.*` / `factory.*` / `trade.*`(外贸主体) | 名称地址等 |
| `calc.*` | 含税单价、含税金额、二级加价单价、保价单价/金额（规则引擎产物） |
| `meta.*` | 今天、基准周五（多种格式） |

编号规则占位符：`{brand2}`品牌前2位大写、`{fc}`仓代码、`{date}`合同日期、`{country}`、`{shipdate}`，主体级配置，如 `BY-SA-{brand2}-{fc}-{date}`。

## 6. 模板映射配置

两种定位方式（两套 RPA 各验证了一种，都要支持）：

```jsonc
{
  "sheets": [{
    "name": "模板",
    "cells": { "B5": "shipment.fc_code", "B16": "shipment.carton_num" },   // ① 固定坐标
    "anchors": [                                                            // ② 锚点行（盈和式）
      { "find": "客户单号", "in_column": "A", "offset_col": "L", "value": "shipment.ship_sn" }
    ],
    "tables": [{                                                            // ③ 明细区
      "source": "items",            // items / boxes
      "start_row": 18, "insert_rows": true,
      "columns": { "A": "shipment.fba_id", "G": "item.name_customs_cn", "J": "item.qty" }
    }]
  }],
  "granularity": "shipment",        // shipment=每货件一份 / batch=每批一份 / row_per_shipment=每货件一行
  "filename": "{brand}-托书-{fc}-{shipdate}"
}
```

固定文字用 `text:` 前缀；`{a}/{b}` 格式串拼接；写入永远显式指定 sheet（杜绝 b16 写错工作表那类事故）。

## 7. 业务流程（界面走向）

```
① 批次管理 ─ 同步赛狐 → 选入库计划 → 自动拉全 → 校验报告
② 核对页   ─ 批次/货件/明细/箱 数据总览，红色标缺失项，可改可补
③ 生成     ─ 按货件的货代自动勾选模板集 → 一键生成（托书+报关+采购合同×2+9810）
            → output/{批次}/{货件}/ 归档，可单下/打包
④ 回填     ─ 货代下单后，在货件上填"货代单号" → 生成投保文件
⑤ 完成     ─ 状态流转留痕，历史批次可随时重新生成（数据/模板版本都在）
```

每页右上常驻「校验」入口；生成失败逐模板报错（哪个模板哪个字段缺什么），不影响其他文件。

## 8. 模块与页面清单

| 模块 | 功能 |
|---|---|
| 批次管理 | 同步/导入、批次列表、货件详情、核对、生成、回填货代单号、归档下载。**校验通过是生成的前置条件（强制）**；数据允许界面修改，改后以系统保存为准，重新同步不覆盖人工修改字段 |
| 产品库 | 报关主数据 CRUD + Excel 导入（兼容现有"商品列表-报关"表头） |
| **工厂管理** | 独立 CRUD + Excel 导入 |
| 主体与品牌 | 三个标签页：店铺主体 / 外贸主体(星盟) / **品牌绑定**（品牌→工厂+主体，含编号规则预览） |
| 货代 & 模板 | 货代 CRUD、模板上传、**AI 映射助手**（自动生成映射草稿+置信度标注）、映射配置（字段字典面板）、试生成、模板版本留档 |
| 规则配置 | §3.3 全部规则的查看与修改（含修改记录） |
| 生成记录 | 全局文件流水，按批次/货件/类型筛选 |

## 9. 实施计划

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| P1 数据层 | 数据模型 + 赛狐同步 + 产品/工厂/主体/规则模块 | 同步真实批次，核对页数据与赛狐后台一致 |
| P2 生成引擎 | 字段字典 + 规则引擎 + 模板填充（坐标+锚点）+ 归档 | 用 Serenorch-6.5 历史批次复刻出与 RPA 成品**逐单元格一致**的全套文件 |
| P3 收尾 | 投保二阶段、打包下载、Excel 兜底导入、校验完善 | 一个新批次全程不碰影刀走完 |
| P4 增强（可选） | 外箱标 PDF 自动拉取（赛狐 `getLabels.json`）、9810 直连申报、多用户 | — |

P2 的验收方式特别说明：拿历史批次的真实数据跑新系统，diff 新旧产出文件，差异逐个确认是"RPA 的 bug"还是"新系统的 bug"。

## 10. 待确认问题

| # | 问题 | 影响 |
|---|---|---|
| 1 | 投保"原单号/运输工具/提单号"：跨运通 RPA 填 FBA 号，Serenorch 成品填货代单号 XX177，哪个是对的？（决定投保是否必须等货代回单） | 流程④ |
| 2 | 编号规则确认：推测 货件级=`[SA-]{brand2}-{fc}-{date}`、采购合同=`[SA-]{brand2}-{country}-{date}`，带 SA 前缀=经星盟的二级链路。按品牌逐一确认（`BY-` 前缀含义仍未明） | 主体/品牌配置初始化 |
| 3 | 托书"产品申报单价"RPA 里疑似写死 `2`，是巧合还是规则？ | 映射来源 |
| 4 | 9810 的"计量单位代码 140=盒"等是否全品类固定？ | 9810 模板映射 |
| 5 | 嘉欣→玫玑研那票只有一级采购合同；二级（→星盟）什么条件下才生成？（与问题2的 SA 前缀联动） | 采购合同生成条件 |

**已拍板的决策**：① 校验通过是生成的强制前置条件；② 批次数据允许界面修改、以系统保存为准（同步不覆盖人工修改）；③ 工厂独立模块；④ AI 映射助手纳入正式范围（模板三步向导：上传→AI草稿+人工确认→试生成）。
**已到位的数据**：店铺主体-工厂信息库.xlsx（品牌→工厂+主体绑定，5品牌/3工厂/5主体+星盟SA），将作为主体与品牌模块的初始化导入格式。

---

**评审通过后**：先建 P1 骨架（一天内可看到批次同步真实跑通），P2 用历史批次做逐单元格比对验收。
